import axios from "axios";
import { userSign } from "../pages/Auth";

export const userSignup = async (data: userSign) => {
  // try {
  //     const response = await axios.post(`https://front-mission.bigs.or.kr/auth/signup`)
  // } catch (error) {
  // }
};
